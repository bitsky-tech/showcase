# All workflows

Every workflow below is also served as an API. Card order matches the desktop app.

<WorkflowGrid />

## Where this comes from

This page and the home page both read `api/workflows.en.json` — the same payload an API consumer gets, so the pages and the API cannot disagree. See the [API reference](/en/api).
